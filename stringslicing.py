a = "amazing"

#print(a[:7]) #prints all the 7 characters in the string a 
#print(a[1: 7: 2]) #prints every second character starting with index 1 from the whole word 
#print(a[0:]) #prints all characters starting from the first character at index 0
#print(a[0:7:3]) or print(a[::3]) #prints every third character from the whole word starting with the first character at index 0
#print(a[::-1]) # prints string a in reverse
#print(a[::-2]) # prints every second character starting with the last character